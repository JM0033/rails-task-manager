<h1>I'm the show view</h1>

<h2><%= @task.title %></h2>
<p>Details : <%=  @task.details %></p>
<p>Completed ? : <%=  @task.completed %></p>

<%= link_to 'edit', task_edit_path(@task) %>
<%= link_to 'delete',
task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
